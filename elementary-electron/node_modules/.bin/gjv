#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../geojson-validation/bin/gjv" "$@"
  ret=$?
else 
  node  "$basedir/../geojson-validation/bin/gjv" "$@"
  ret=$?
fi
exit $ret
